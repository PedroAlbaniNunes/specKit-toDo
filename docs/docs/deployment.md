# Implantação

A aplicação pode ser hospedada em serviços gratuitos como Render ou Railway.

A documentação MkDocs pode ser publicada com GitHub Pages ou Vercel.

Para implantação local, é suficiente instalar as dependências com `pip install -r requirements.txt` e iniciar a aplicação com `python flask_app.py`.
